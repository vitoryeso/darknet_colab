    precisamos aplicar o comando de teste do darknet usando cada arquivo de peso da pasta weights,
no conjunto de dados de validação.
    cada vez que o darknet validar, vai gerar um output em texto, que tera o mAP geral, de cada classe,
e o IOU
    tudo isso será salvo em csv.

    será feito em shellscript, com a parte de filtragem do output em texto feito em python.


    echo "avg_mAP;mAP_car;mAP_bus;mAP_mot;avg_IOU" > cfg.csv # maybe IOU per class...
    for each weight_file:
        prov=$(./darknet blablabla > get_valid_info.py) # this is a string like: "70.21;88.09;..." etc 
        # here u append this line in cfg.csv including "\n" 
